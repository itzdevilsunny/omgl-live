# Pusher credentials — get from https://pusher.com (free plan works great)
PUSHER_APP_ID=your_app_id
PUSHER_SECRET=your_secret

# These are also exposed to the browser (NEXT_PUBLIC prefix)
NEXT_PUBLIC_PUSHER_KEY=your_key
NEXT_PUBLIC_PUSHER_CLUSTER=us2
